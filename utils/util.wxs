var substrYearMonth = function (val) {

  if (val.length == 0 || val == undefined) {

    return false;

  } else if (val.length > 0) {

    return val.substring(0, 8) + "...";

  } else {

    return val;

  }

}


module.exports.substrYearMonth = substrYearMonth